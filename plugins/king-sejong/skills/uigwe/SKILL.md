---
name: uigwe
description: Use when a user explicitly invokes `uigwe`, `의궤`, or wants a vague goal, partial brief, approved design, or packet set turned into formal planning artifacts using auto, full, design-to-plan, or decompose-only.
---

# Uigwe Plugin Adapter

This plugin-scoped skill is a discovery adapter. It is not the durable King Sejong planning contract.

Load and follow `${CODEX_HOME:-~/.codex}/skills/uigwe/SKILL.md`, using shared docs from `${CODEX_HOME:-~/.codex}/skills/sejong/docs/`.

If those installed files are missing, tell the user to run:

```bash
bash scripts/install-sejong.sh --scope user --force
```

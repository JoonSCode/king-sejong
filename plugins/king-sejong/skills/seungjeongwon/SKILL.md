---
name: seungjeongwon
description: Use when a user invokes Seungjeongwon/승정원 or when Sejong needs native execution, verification, execution feedback, commit-ready evidence, or a persistent completion loop for a clear task or validated Uigwe bundle.
---

# Seungjeongwon Plugin Adapter

This plugin-scoped skill is a discovery adapter. It is not the durable King Sejong execution contract.

Load and follow `${CODEX_HOME:-~/.codex}/skills/seungjeongwon/SKILL.md`, using shared docs from `${CODEX_HOME:-~/.codex}/skills/sejong/docs/`.

If those installed files are missing, tell the user to run:

```bash
bash scripts/install-sejong.sh --scope user --force
```

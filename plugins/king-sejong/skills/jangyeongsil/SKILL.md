---
name: jangyeongsil
description: Use when a user invokes JangYeongsil/장영실 or when Sejong, Uigwe, or Jiphyeonjeon needs bounded research, experiment, source inspection, repo-history review, example gathering, or evidence separation before a decision.
---

# JangYeongsil Plugin Adapter

This plugin-scoped skill is a discovery adapter. It is not the durable King Sejong research contract.

Load and follow `${CODEX_HOME:-~/.codex}/skills/jangyeongsil/SKILL.md`, using shared docs from `${CODEX_HOME:-~/.codex}/skills/sejong/docs/`.

If those installed files are missing, tell the user to run:

```bash
bash scripts/install-sejong.sh --scope user --force
```

---
name: jiphyeonjeon
description: Use when a user invokes Jiphyeonjeon/집현전 or when Sejong, Uigwe, JangYeongsil, or Seungjeongwon needs structured discussion, option comparison, adversarial review, council-style decision support, or rejected-option handling.
---

# Jiphyeonjeon Plugin Adapter

This plugin-scoped skill is a discovery adapter. It is not the durable King Sejong discussion contract.

Load and follow `${CODEX_HOME:-~/.codex}/skills/jiphyeonjeon/SKILL.md`, using shared docs from `${CODEX_HOME:-~/.codex}/skills/sejong/docs/`.

If those installed files are missing, tell the user to run:

```bash
bash scripts/install-sejong.sh --scope user --force
```
